/* header file containing general elements and features*/

struct Position {
    // The x position
    int x;
    // The y position
    int y;
};

struct Velocity {
    // The x velocity
    int x;
    // The y velocity
    int y;
};